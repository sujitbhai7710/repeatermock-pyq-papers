# The agent runs on the Python standard library only.
#
# This file is intentionally a *no-op*: `pip install -r requirements.txt`
# succeeds without installing anything, which is exactly what the GitHub Action
# relies on (the runner never needs a third-party package).  Keep every entry
# commented out — an active entry here would mean the pipeline is no longer
# stdlib-only.
#
# Optional developer conveniences (not needed to run the agent, the tests or
# the tools):
#
#   - types-* packages improve editor/type-checker hints
#   - pytest is optional: the shipped tests use unittest (stdlib)
#
# Install with:  pip install -r requirements.txt
#
# types-PyYAML==6.0.12.20240917
# pytest==8.3.3
